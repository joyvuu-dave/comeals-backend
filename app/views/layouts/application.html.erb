<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="Post your common meal. Sign up for others. Reconcile the costs.">
    <title>Comeals</title>
    <%= stylesheet_pack_tag 'app' %>
    <%= stylesheet_link_tag 'application', {rel: "preload", as: "style", onload: "this.rel='stylesheet'"} %>
  </head>
  <body>

    <div id="main">
      <%= yield %>
    </div>

    <%= render 'partials/footer' %>

    <% if @community&.id.present? && @resident_id.present? %>
      <%= render 'partials/calendar_subscribe' %>
    <% end %>

    <%= javascript_pack_tag 'app', defer: true %>

    <% if Rails.env.production? %>
      <script>
        (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
        (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
        m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
        })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

        ga('create', 'UA-105981750-1', 'auto');
        ga('send', 'pageview');
      </script>
    <% end %>
  </body>
</html>
