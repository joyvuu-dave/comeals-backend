<%= content_tag :div,
  id: "site-data",
  data: {
    production: Rails.env.production?,
    id: @meal&.id,
    date: @meal&.date,
    email: @email,
    token: @token,
    community_id: @community&.id,
    version: @version
}.to_json do %>
<% end %>
