<%= content_tag :div,
  id: "site-data",
  data: {
    production: Rails.env.production?,
    id: @meal&.id,
    date: @meal&.date,
    email: @email,
    name: resident_name_helper(@resident&.name),
    hosts: @hosts,
    token: @token,
    community_id: @community&.id,
    version: @version
}.to_json do %>
<% end %>
